# 起航研学手册 GUI — 开发过程记录

**日期**：2026-05-30  
**开发模式**：无头模式（Headless）

---

## 项目演进

| 版本 | 分支 | 目录 | 框架 | 状态 |
|------|------|------|------|------|
| v0.1 | `v0.1` | `起航研学手册GUI项目` | Vite + React + Electron CJS | ✅ 稳定 |
| v1.0 | `main` | `起航研学手册GUI桌面版` | Vite + React + Electron ESM | 备选 |
| v2.0 | `v2.0` | `起航研学手册GUI-V2` | Vite + React + Electron ESM | 🟡 开发中 |

---

## 一、v0.1 版本（旧版）

### 初始状态
- React 19 + TypeScript + Vite 7
- 手册编辑器：页面切换、元素选中、属性编辑、HTML/PNG 导出
- ~831 行代码，30 个模块

### 功能新增与 Bug 修复

| # | 类型 | 内容 |
|---|------|------|
| 1 | 架构 | Electron 框架集成（main.cjs + preload.cjs）|
| 2 | 功能 | 菜单栏：文件/编辑/帮助 + 快捷键 Ctrl+N/O/S/Z/Y/Q |
| 3 | 功能 | 文件对话框：打开/保存项目、导出 HTML/PNG |
| 4 | 功能 | IPC 通信（文件读写、最近文件列表）|
| 5 | 功能 | Windows 11 特性：窗口状态保存、单实例锁 |
| 6 | Bug | App.tsx 缺少 `electronAPI` 导入 → 添加 |
| 7 | Bug | `electron-integration.ts` 内存泄漏 → useRef 模式 |
| 8 | Bug | 删除死代码 `file-operations.ts` |
| 9 | Bug | `{{slot}}` UI 显示错误 → `{'{{slot}}'}` |
| 10 | Bug | `project.name` → `project.projectName` |
| 11 | 优化 | 移除未使用 `electron-store` 依赖 |
| 12 | 安全 | Vite host `0.0.0.0` → `localhost` |
| 13 | 重写 | `scripts/copy-electron-files.cjs`：崩溃日志 `~/.qihang-crash-logs/crash.log` |
| 14 | Bug | 图层上移/下移方向颠倒 → delta 值交换 |
| 15 | 功能 | 素材库与页面列表换位（左边栏=页面列表，右边栏=素材库） |
| 16 | 功能 | 素材删除按钮（hover 显示 ×）|
| 17 | 功能 | 关闭窗口保存确认对话框（三选一）|
| 18 | Bug | 示例图片路径 `/assets/` → `./assets/`（打包后生效）|
| 19 | Bug | 无 slot 模板导出无效 → `/\{\{/.test(html)` 判断降级元素渲染 |
| 20 | 功能 | HTML 模板元素选择器扩充（8 种 → 24 种）|
| 21 | 文档 | 清理 11 个重复文档，新增 BUGFIX_SUMMARY.md、COMPREHENSIVE_AUDIT.md |

### 当前已知问题
- Linux 上打包需要 Wine（已生成 win-unpacked 可用）
- 导入无 `{{slot}}` 模板后画布布局与原 CSS 差异大
- ~~无 slot 模板导出编辑不生效~~ → 已修复

---

## 二、v1.0 版本（桌面版）

### 架构改进
- ESM 模块系统（`main.mjs` + `preload.mjs`）
- `electron/main.ts` TypeScript 源码 + esbuild 编译
- 构建输出 `dist-electron/` 与前端 `dist/` 分离
- electron-builder 输出 `release/`（不与 Vite 冲突）
- Electron 34.x，支持 Windows 11 Mica 材质

### 构建脚本
```bash
npm run dev          # 开发模式
npm run build        # 生产构建（renderer + electron）
npm run dist:win     # 打包 Windows 安装包
```

---

## 三、v2.0 版本（开发中）

### 基于 v1.0 架构，新增：

1. **HTML 源码编辑面板**
   - 工具栏「编辑源码」按钮
   - 500px 高等宽字体 textarea
   - 显示当前页 `buildHtml()` 输出
   - 「应用修改」保存到 `page.templateHtml`

2. **崩溃安全日志**（同 v0.1）
   - 路径：`%USERPROFILE%\.qihang-crash-logs\crash.log`
   - 从 `main.ts` 第一行即写日志
   - 捕获 `uncaughtException`、`did-fail-load`、`render-process-gone`

3. **窗口显示修复**
   - `show: false` → `show: true`
   - `did-fail-load` 错误降级页面

### 当前待排查
- 🟡 Windows 打包后 exe 双击无窗口显示

---

## 四、技术债务与后续方向

| 优先级 | 事项 |
|--------|------|
| 🔴 | V2 exe 启动无窗口问题 |
| 🟡 | 模板导入后保留原始 CSS 布局 |
| 🟡 | 单元测试覆盖 |
| 🟡 | 应用图标 |
| 🟢 | 自动更新 |
| 🟢 | CI/CD 构建流水线 |

---

## 五、关键文件对照

| 功能 | v0.1 | v2.0 |
|------|------|------|
| 主进程 | `dist/main.cjs` | `electron/main.ts` → `dist-electron/main.mjs` |
| 预加载 | `dist/preload.cjs` | `electron/preload.ts` → `dist-electron/preload.mjs` |
| 前端入口 | `src/App.tsx` | `src/App.tsx` |
| 构建 | `npm run build` + `copy-electron-files.cjs` | `npm run build` + `build-electron.mjs` |
| 打包 | `npm run build:win` → `dist/` | `npm run dist:win` → `release/` |
| 崩溃日志 | `~/.qihang-crash-logs/crash.log` | 同 |

---

## 六、2026-05-31 修复记录

### 本次处理内容
1. **本地开发启动修复**
   - `npm run dev` 原先直接调用 `electron . --dev`，在当前 Linux 环境下触发 `chrome-sandbox` 权限报错
   - `dev` 脚本切换为统一走 `scripts/dev-electron.mjs`
   - Electron 启动参数在 Linux 下追加 `--no-sandbox`

2. **运行时 bug 修复**
   - 空白区域点击后无法真正取消选中：`selectedElement` 不再对空选中状态做自动回填
   - Electron 菜单事件监听重复注册：预加载层返回 `unsubscribe`，Renderer 在 `useEffect` 清理
   - 图片导入 MIME 固定为 PNG：按文件扩展名推断 `image/jpeg`、`image/webp`、`image/gif`、`image/svg+xml` 等类型

3. **类型与构建修复**
   - 新增 `src/global.d.ts`，补全 `window.electronAPI` 的类型声明
   - 移除 `src/templateRegistry.ts` 中引用的缺失模板资源，避免 `tsc` 失败

### 验证结果
- `npm run dev` 已可正常拉起 Vite + Electron
- `npx tsc --noEmit` 通过
- `npm run build` 通过

### 备注
- 当前仍保留后续可优化项：源码编辑草稿同步、导出 HTML 的资源内嵌策略
